# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json

name: samsonite-os
description: Samsonite OS - Main (Intel/AMD)
base-image: ghcr.io/ublue-os/bazzite-gnome
image-version: latest # This will be overruled by the branch name during the build

modules:
  - from-file: _common-modules.yml
